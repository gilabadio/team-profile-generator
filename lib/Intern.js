class Intern{
    constructor(name = ""){
        this.name= name;
        this.id = [i];
        this.email = email;
        this.school = school;
    }

    getName() {
        return `Employee Name: ${this.name}`;
    }

    getId(){
        return`ID: ${this.id}`;
    }

    getEmail() {
        return `Email: ${this.email}`;
    }
    getRole(){
        return `Intern`;
    }
    getSchool(){
        return `School: ${this.school}`;
    }
}